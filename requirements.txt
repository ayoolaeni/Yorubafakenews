# Core data handling
pandas>=2.0
numpy>=1.24

# Scraping
requests>=2.31
beautifulsoup4>=4.12
selenium>=4.15
feedparser>=6.0
lxml>=4.9

# Text processing
regex>=2023.10.3
nltk>=3.8

# Classical ML
scikit-learn>=1.3
imbalanced-learn>=0.11

# Deep learning / transformers
torch>=2.1
transformers>=4.35
datasets>=2.15
accelerate>=0.25
sentencepiece>=0.1.99  # required by AfriBERTa/AfroXLM-R's tokenizers

# Embeddings
gensim>=4.3
# fasttext (official) needs a C++ build toolchain and often fails to build on
# Windows. static_embeddings.py loads fastText .vec/.bin vectors via gensim
# (KeyedVectors), so the fasttext package itself is optional:
# fasttext-wheel>=0.9.2  ; uncomment if you want the native fasttext trainer/loader

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Web
streamlit>=1.28
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.4

# Config / utils
pyyaml>=6.0
python-dotenv>=1.0

# Testing
pytest>=7.4
pytest-cov>=4.1
